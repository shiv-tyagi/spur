---
- name: Spur CI host provisioning
  hosts: gpu_hosts
  become: true
  roles:
    - storage
    - kernel_iommu
    - ci_user
    - virtualization
    - gim_driver
    - amdgpu_blacklist
    - libvirt_networks
    - scripts_deploy
    - network_egress
    - runner
    - reaper
    - fail2ban
    - guest_image
